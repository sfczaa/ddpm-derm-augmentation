# Hugging Face Space (Gradio, ZeroGPU tier).
#
# Unlike requirements-deploy.txt this does not pin the CPU-only torch wheels:
# ZeroGPU hardware uses the CUDA build from PyPI. Inference itself still runs
# on CPU (see app.py).
spaces>=0.30
gradio>=6.16.0,<7
torch>=2.13.0,<3
torchvision>=0.17,<1
numpy>=1.24,<3
pillow>=12.3.0,<13
huggingface_hub>=0.23,<1
