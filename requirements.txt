# Local (smoke test only) needs just these three:
#   pip install pandas numpy pillow
# The full training/inference stack below is intended for Colab (T4 GPU).
# On Colab, torch + torchvision are preinstalled; you usually only need pandas.

pandas>=2.0
numpy>=2.5.2
pillow>=12.3.0

# --- Colab / GPU training (already present on Colab, listed for reproducibility) ---
torch>=2.13.0
torchvision>=0.15

# --- frozen CoCa second classifier (Colab validation downloads real weights) ---
open_clip_torch==3.3.0

# --- frozen-CoCa representation diagnostic: fixed logistic-regression probe only ---
scikit-learn==1.8.0

# --- PanDerm-Base C1 full fine-tuning (Colab; weights are NOT redistributable) ---
# timm is pinned to the version required by the upstream PanDerm checkout, which
# supplies both the model definition and the training preprocessing. gdown fetches
# the official checkpoint; its SHA-256 is verified before any training runs.
timm==0.9.16
gdown>=5.2.2

# --- stage 2: DDPM generator (Colab) ---
diffusers>=0.38.0      # UNet2DModel (class-conditional noise predictor)

# --- stage 4 deployment ---
# See requirements-deploy.txt for the CPU FastAPI container dependencies.
